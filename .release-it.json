{
  "hooks": {
    "after:bump": "cd ./dist/ && cat ../app.json | jq '.expo.version |= \"${version}\"'"
  },
  "git": {
    "commitMessage": "chore: release ${version}",
    "tagName": "${version}"
  },
  "github": {
    "release": false,
    "draft": true,
    "preRelease": false,
    "web": true,
    "autogenerate": true
  }
}
