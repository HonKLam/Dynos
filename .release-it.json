{
  "hooks": {
    "after:bump": "cd ./dist/ && cat ../app.json | jq '.expo.version |= \"v${version}\"' > app.tmp && mv app.tmp app.json"
  },
  "git": {
    "commitMessage": "chore: release v${version}",
    "tagName": "v${version}"
  },
  "github": {
    "release": true,
    "draft": true,
    "web": true,
    "autogenerate": true
  }
}
